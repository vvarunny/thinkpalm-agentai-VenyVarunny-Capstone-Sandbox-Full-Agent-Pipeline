// Test cases for short typo detection

pri("Hello");                 // Should detect as typo for "print"
pr("World");                  // Should detect as typo for "print"
ln([1,2,3]);                  // Should detect as typo for "len"
rn(5);                        // Should detect as typo for "range"
in("input");                  // Should detect as typo for "input"

// Function name abbreviations
cons.log("test");             // Should detect as typo for "console.log"
ale("alert");                 // Should detect as typo for "alert"
pro("prompt");                // Should detect as typo for "prompt"

// Python examples
pri("Python test");           // Should detect as typo for "print"
ln([1,2,3,4]);               // Should detect as typo for "len"
rn(10);                       // Should detect as typo for "range"
dic({"key": "value"});       // Should detect as typo for "dict"

// C# examples
Con("C# test");               // Should detect as typo for "Console"
Wr("Write");                  // Should detect as typo for "Write"
Re("Read");                   // Should detect as typo for "Read"
